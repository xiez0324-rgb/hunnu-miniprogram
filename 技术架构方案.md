# 家教小程序平台 · 技术架构方案

| 项 | 内容 |
|---|---|
| 文档版本 | V1.0（待确认） |
| 创建日期 | 2026-09-03 |
| 关联文档 | PRD.md V1.0、界面布局文档.md V0.2 |
| 配套资源 | 1号开发版本/hunnu-miniprogram/taro-miniapp（Taro + 云函数基础工程） |
| 前端蓝本 | 二号开发版本（Vite 高保真预览，已定稿 UI/交互） |

---

## 1. 架构决策记录（已确认）

| 决策点 | 结论 |
|---|---|
| 产品载体 | **微信小程序仅含「老师端 + 家长端」双角色**；**管理员单独做 Web 网站**，两者不混装 |
| 后端方案 | **微信云开发（CloudBase）**：云函数 + 云数据库 + 云存储，个人主体免备案 |
| 资源共用 | 小程序与管理员 Web **共用同一个云环境、同一批云函数、同一套数据库** |
| 小程序前端 | **演进 1号开发版本 的 Taro 工程**（Taro 4.1.9 + React + TS），UI 按二号定稿重建 |
| 管理员前端 | **独立 React + Vite 工程**（以二号 AdminPanel 高保真预览为蓝本），替换 mock 为云函数调用 |
| 管理员登录 | CloudBase「用户名密码登录」，自建 `admins` 集合鉴权，不走微信授权 |
| 数据安全 | **浏览器/小程序端永不直连数据库写库**；所有写操作收敛到云函数，云函数内校验身份 |
| 费用模式 | 平台不碰钱，小程序无支付；8% 信息费由代理人微信私信线下收取，后台登记 |

---

## 2. 总体架构

```
┌──────────────────────────┐   ┌───────────────────────────┐
│  微信小程序（双角色内切）    │   │  管理员 Web 网站（独立工程） │
│                          │   │                           │
│  🎓 老师端 Tab1/2/3       │   │  仅管理员账号可登录          │
│  🏠 家长端 Tab1/2/3       │   │  订单/审查/投递/收费/风控     │
│  「我的」页内切换身份        │   │  可增删改业务数据            │
│                          │   │                           │
│  wx.cloud.callFunction()  │   │  @cloudbase/js-sdk         │
└───────────┬──────────────┘   └───────────┬───────────────┘
            │                              │
            └──────────────┬───────────────┘
                           ▼
        ┌────────────────────────────────────┐
        │  云函数层（唯一数据入口）             │
        │  业务云函数 + adminXxx 管理云函数     │
        │  所有 adminXxx 首行执行 requireAdmin │
        └──────────────────┬─────────────────┘
                           ▼
        ┌────────────────────────────────────┐
        │  云数据库（对外关闭读写，只信云函数）  │
        │  users/demands/applications/...     │
        └────────────────────────────────────┘
```

### 2.1 关键约束（纪律）

1. **双前端 → 单后端**：两套前端共用云函数，业务规则只写一遍，天然保证状态一致。
2. **写库只走云函数**：小程序端与 Web 端 SDK 拿到的都是「普通用户」身份，被数据库安全规则挡住；云函数持有管理端权限，负责全部读写。
3. **管理员权限收口**：`requireAdmin(event)` 是每个管理云函数的第一行，防止越权调用。
4. **小程序内无管理员代码**：AdminPanel 等组件只存在于二号预览（演示用）与 Web 工程，不进入 Taro 工程。

---

## 3. 前端工程规划

### 3.1 小程序工程（演进一号）

| 项 | 方案 |
|---|---|
| 框架 | Taro 4.1.9 + React 18 + TypeScript + Sass |
| 页面容器 | 老师端：需求广场 / 我的报名 / 我的；家长端：发布需求 / 我的需求 / 我的 |
| 角色模型 | 首次进入选择身份；「我的」页底部切换身份，切换后进入对方端 tabBar |
| UI 蓝本 | 二号定稿（撕纸风卡片、二次确认弹窗、独立简历页、性别偏好、联系方式页、开发者的承诺等逐项迁移） |
| 数据访问 | `src/services/cloud.ts` 封装 `callFunction`；非小程序环境（H5/开发预览）走 `src/data/*` mock |

一号工程已有可复用资产：15 个云函数骨架、`callFunction` 封装、双端页面结构、认证/简历组件。

### 3.2 管理员 Web 工程（新建）

| 项 | 方案 |
|---|---|
| 技术栈 | React 18 + Vite + TS（与二号预览同栈，便于平移 UI） |
| UI 蓝本 | 二号 AdminPanel（高信息密度、深色侧栏 + 白底内容区、支持移动端） |
| 数据访问 | `@cloudbase/js-sdk` → `app.callFunction({ name: "adminXxx" })`，**不直连数据库** |
| 登录 | CloudBase 用户名密码登录 → 登录态存本地 → 路由守卫 |
| 托管 | 云开发静态网站托管（默认域名可起步）或 Vercel |

> 注：二号原型右上角「管理员后台 →」切换按钮、AdminPanel 组件仅用于**预览演示**；真实交付中管理员界面从二号工程中拆出，独立成 Web 工程。

---

## 4. 云开发后端设计

### 4.1 数据集合清单（云数据库）

| 集合 | 说明 | 关键字段 |
|---|---|---|
| `users` | 用户（老师/家长双角色合一，role 区分） | openid、role、name、avatar、phone、area、wechat、contacts（我的联系方式）、createdAt |
| `teachers` | 老师档案/认证信息 | userId、school、verified（已认证）、subjects、grades、times、rate、area、intro（完整简历）、verifyMaterials |
| `demands` | 需求单 | parentId、grade、subject、category、goal、time、budget、area、location(必填)、gender(性别偏好)、note、status(进行中/已成交/已下架) |
| `applications` | 报名记录 | demandId、teacherId、status(已报名/已推荐/已确认/已成交/已取消)、createdAt |
| `matches` | 匹配/推荐记录 | demandId、applicationId、operatorId(代理人)、recommended、confirmedAt、操作留痕 |
| `orders` | 订单（家长确认后生成，即后台"待处理订单"） | demandId、teacherId、parentId、双方联系方式快照、status(待联系/已对接/已成交/已取消)、operatorId |
| `fee_records` | 信息费台账 | orderId、totalFee、fee8(8%)、status(待付/已付)、wxFlowId(微信流水号) |
| `notifications` | 站内信 | userId、type、title、body、unread、createdAt |
| `admins` | 管理员账号 | username、passwordHash、uid(CloudBase 登录用户)、level、enabled |
| `audit_logs` | 后台操作留痕 | operatorId、action、target、detail、createdAt |

跨端同步规则（示例）：
- 家长在详情页确认人选 → `confirmMatch` 云函数：应用状态置「已确认」+ 生成 `orders` 记录（status=待联系）→ 管理员 Web 仪表盘实时读到新待处理订单。
- 老师端未读数 / 家长端角标 = 读 `notifications` 中本人 unread>0 的数量；已读/全部已读写库后，各端计数同步消失。

### 4.2 云函数清单

按一号现有骨架补齐、对齐二号数据模型：

| 类别 | 云函数 |
|---|---|
| 账号 | `login`（微信登录+手机号绑定）、`saveMyContact`（双端联系方式，手机号+区域必填）、`adminLogin` |
| 老师端 | `saveResume`、`submitVerification`、`getDemands`、`getDemandDetail`、`applyDemand`、`cancelApplication`、`getMyApplications`、`getFeeRecords` |
| 家长端 | `createDemand`、`getMyDemands`、`getApplicants`、`confirmMatch`、`cancelConfirm` |
| 管理端（`adminXxx`，首行 `requireAdmin`） | `adminListOrders`、`adminUpdateOrder`、`adminReviewVerify`、`adminRecommend`、`adminRegisterFee`、`adminListDeliveries`（投递去向+风控提示）、`adminUnlockContact`、`adminDashboard` |
| 通知 | `markRead`、`markAllRead`、`getNotifications` |

### 4.3 管理员鉴权方案

```
云开发控制台：开启「用户名密码登录」
        │
        ▼
管理员网页登录 → CloudBase 返回 uid（登录用户身份）
        │
        ▼
调用 adminXxx(event) → 云函数内 requireAdmin(event)：
  const { OPENID } = cloud.getWXContext();      // 小程序侧
  const uid = event.userInfo?.uid;              // Web 侧
  → 查 admins 集合（uid 或 username 匹配）
  → 非管理员 → 抛「无管理员权限」
  → 是管理员 → 放行并继续执行（写 audit_logs）
```

### 4.4 数据权限（数据库安全规则）

| 集合 | 客户端规则（小程序/Web） | 实际效果 |
|---|---|---|
| `demands` 等业务集合 | 仅读本人/公开字段，**禁止客户端写** | 所有变更经云函数 |
| `orders` / `matches` / `fee_records` / `audit_logs` | **全部拒绝客户端读写** | 仅云函数（管理端权限）可操作 |
| `admins` | 全部拒绝客户端读写 | 密码哈希只进不出 |

---

## 5. 网页连接云端数据库（接入要点）

1. 安装 Web SDK：`npm i @cloudbase/js-sdk`。
2. 初始化：`cloudbase.init({ env: "环境ID" })`。
3. 登录：`auth.signInWithUsernameAndPassword(...)`（需控制台先开启该登录方式）。
4. 调数据：一律 `app.callFunction({ name: "adminXxx", data })`。
5. 域名白名单：开发期加入 `http://localhost:*`，上线加入正式域名。
6. 环境归属：微信公众平台开通的小程序云环境，需绑定腾讯云账号后可在 CloudBase 控制台管理（Web 接入前提，环境开通后第一个验证点）。

---

## 6. 开发阶段计划

| 阶段 | 内容 | 依赖 |
|---|---|---|
| 0 · 环境准备 | 开通微信云开发环境、获取环境 ID、控制台开启用户名密码登录 | 用户操作 |
| 1 · 云端后端 | 建集合 + 索引；补齐云函数与 `requireAdmin`；首批 `adminXxx` 接口 | 环境 ID |
| 2 · 小程序前端 | 一号 Taro 工程按二号定稿 UI 演进，接通云函数 | 阶段 1 |
| 3 · 管理员 Web | 新建 React 工程，AdminPanel 去 mock 化，接 `adminXxx` | 阶段 1 |
| 4 · 联调部署 | 微信开发者工具上传云函数、真机预览；Web 静态托管 | 阶段 2/3 |

---

## 7. 待用户操作清单

- [ ] 微信公众平台注册小程序 → 获取 AppID（已完成 ✅）
- [ ] 小程序后台 → 开发管理 → 云开发 → 开通免费环境，记录**环境 ID**
- [ ] 环境 ID 同步给开发；确认云环境可在 CloudBase 控制台（tcb.cloud.tencent.com）管理
- [ ] 云开发控制台 → 身份认证 → 开启「用户名密码登录」并配置安全域名
- [ ] 确认二号预览稿为最终 UI 冻结版（后续 UI 微调尽量在进入阶段 2 前完成）

---

## 8. 风险与备注

| 风险 | 说明 |
|---|---|
| 双工程双前端 | 需维持二号（蓝本）、小程序、Web 三方 UI 一致 → 以「界面布局文档 + 二号预览」为唯一事实源，UI 变更先改蓝本再同步 |
| Web 安全域名/登录方式配置遗漏 | 会导致网页调不通云函数 → 阶段 0 部署文档中给出截图级 checklist |
| 数据库安全规则误配 | 一旦放开客户端写库权限将失控 → 默认全关，只靠云函数 |
| 个人主体限制 | 小程序内文案规避交易/收费表述（沿用 PRD 合规策略） |
