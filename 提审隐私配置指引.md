# 提审准备 · 用户隐私保护指引配置 + 隐私协议弹窗接入说明

> 适用工程：`taro-miniapp`（二号开发版本）
> 关联文件：`src/app.tsx`、`src/components/PrivacyGate/*`、`src/services/privacy.ts`、`src/pages/privacy/index.tsx`、`src/app.config.ts`
> 配套后台：小程序公众平台 https://mp.weixin.qq.com （AppID：[AppID 见本地 .env]）

---

## 一、代码已完成的接入（本次改动）

| 项目 | 说明 |
|---|---|
| 自定义隐私协议弹窗 | `src/components/PrivacyGate`：首次启动弹自定义「隐私保护提示」（说明收集范围 + 手机号选填 + 链接到《用户信息采集与隐私保护声明》）。**同意** → 本地记录 + 同步微信官方隐私授权；**不同意** → 引导退出小程序，不收集任何信息 |
| 老用户处理 | 已同意过（本地记录 `privacy_consent_v1`）则静默放行，并在启动时调用 `wx.requirePrivacyAuthorize` 同步微信官方授权 |
| 官方弹窗接入 | `src/services/privacy.ts` `ensureWechatPrivacy()`：`wx.getPrivacySetting` → `needAuthorization=true` 时 `wx.requirePrivacyAuthorize()` 拉起微信官方「隐私保护指引」弹窗（在用户点击自定义弹窗“同意”后触发） |
| 隐私能力开关 | `src/app.config.ts` 增加 `__usePrivacyCheck__: true`（输出到 app.json） |
| 兼容兜底 | 基础库 < 2.32.3 或非小程序端（H5 预览）自动跳过自定义门与官方弹窗，不影响业务 |
| 内部《隐私与风险说明》 | `pages/privacy/index`（8 大条款），与官方弹窗相互独立、文案一致 |

> 设计说明：为通过审核“首次进入需弹隐私协议”的要求，采用「自定义协议门 + 官方授权弹窗」两段式：
> 先向用户完整展示本平台的收集规则并取得同意（不同意直接退出，绝不强索信息），
> 再调用 `wx.requirePrivacyAuthorize()` 完成微信侧授权同步；两者都通过后才进入业务。

---

## 二、小程序后台《用户隐私保护指引》填报清单

路径：公众平台 → **设置 → 服务内容声明 → 用户隐私保护指引 → 更新**。

> 提示：指引每次“更新”需重新提交审核；真机只有 **基础库 ≥ 2.32.3** 且指引处于“已生效”状态，官方弹窗才会正常拉起。

### 2.1 勾选收集项与代码逐项对应

| 后台收集项（按后台选项名称为准） | 建议用途描述（可直接采用） | 对应代码/页面 | 落库位置 |
|---|---|---|---|
| 位置信息（精确地址） | 用于家长发布家教需求时填写上课地址（精确至小区/楼栋），平台据此匹配可上门老师 | 家长发布需求（`Publish` 组件的地址字段）、`demand-detail/parent-demand-detail` 展示 | `demands.area` |
| 手机号（表单自填·选填） | 用于平台代理人与家长/老师对接课程安排与后续沟通，不在广场公开展示；**用户自愿填写，可跳过，未填写不限制浏览与发布** | 我的/个人资料（`contact`）、家长发布需求（`Publish`，选填可预填） | `parent_users`、`teacher_users` 的 `phone`；报名/订单快照 |
| 教育信息（学院/专业；学校固定为唯一合作院校湖南师范大学） | 用于老师资质展示与匹配判断，经授权后以“已认证”标签展示；学校名不在前端展示 | 学籍认证（`verify` 页录入学院/专业）、`teacher-detail` 展示 | `verifications`（school/college/major）、`teachers`、报名快照 `applications` |
| 身份证明材料（学籍/学历截图等） | 仅用于平台人工核验老师身份，不向无关第三方展示 | `verify` 页提交学籍认证（字段/素材上传） | `verifications` |
| 相册（仅当用户主动选择照片上传时读取） | 用于老师从手机相册选择学生证/学信网截图，仅作学籍真实性核验 | `verify` 页「相册选图上传」（`chooseMedia`，`sourceType: ['album']`） | 云存储 `verifications/` → `verifications.materials` 的 fileID |
| 昵称 | 家长/老师称呼展示与区分 | 登录页、`mine` | `parent_users`、`teacher_users` 的 `nickname` |

> ⚠️ 请以后台最新版《用户隐私保护指引》编辑器的**实际选项名称**为准（各类型名称会有调整），上述“后台收集项”为语义对应；务必保证勾选项与第 2.2 节“接口触发类”也一致，避免前后矛盾。
>
> 📵 **手机号与强制索取**：本项目**未调用** `wx.getPhoneNumber`，故后台**不要**勾选“手机号”接口类收集项；表单手填号码为“自填信息”，一律**选填**（代码已取消必填校验），后台说明文字请注明“联系电话为选填，未填写不影响使用”。不得在登录或首次进入时以任何形式强制要求填写手机号。

### 2.2 接口触发类收集（当前代码扫描结果）

小程序目前**未调用**以下隐私类接口，因此**不要**在后台误勾选对应权限，勾了反而构成“声明了却未使用/无法自证”风险：

| 隐私接口 | 当前状态 | 说明 |
|---|---|---|
| `wx.getUserProfile` / 头像昵称填写 | 未使用 | 昵称目前为表单手动填写，未走官方头像昵称能力 |
| `wx.getPhoneNumber` | 未使用 | 电话为表单手动填写，未使用手机号快速验证组件 |
| `wx.chooseImage/chooseMedia`（相册/摄像头） | **已使用（仅相册）** | 学籍认证页 `verify` 通过 `chooseMedia` 从**相册**选学生证/学信网截图，需在后台勾选「相册」类收集项；`sourceType` 仅 `['album']`，**未用摄像头**，摄像头权限无需勾选 |
| `wx.getLocation/chooseLocation` | 未使用 | 地址为文本填写，未使用地图选点 |
| 麦克风/蓝牙/运动数据等 | 未使用 | — |
| `open-type="contact"` 在线客服 | 非隐私接口 | 客服会话由微信托管，无需勾选收集项（见页面 `parent-demand-detail` 的“联系代理”按钮） |

> 结论：当前小程序**仅有的接口触发型敏感收集是「从相册选择学籍材料照片」**（`chooseMedia` 仅 album）；其余个人信息（位置/电话/教育信息等）均走表单手动填写，不属于接口触发型。隐私弹窗在启动时做合规预检与官方授权记录；`chooseMedia` 相册选择依赖后台勾选「相册」类收集项并通过审核后才能拉起授权。

### 2.3 用途描述一致性要求

后台填写的“处理目的”，须与本工程 `src/pages/privacy/index.tsx` 的《用户信息采集与隐私保护声明》第 1~2 条（收集范围 / 使用目的）表述一致，建议直接复制本表“建议用途描述”。

---

## 三、提审前自检清单

1. [ ] 后台已配置《用户隐私保护指引》并处于“已生效”（非草稿）
2. [ ] 指引勾选范围与 §2.1 / §2.2 一致，不虚报不遗漏；**未**勾选“手机号”接口类
3. [ ] 首次启动自定义「隐私保护提示」弹窗已生效（`PrivacyGate`）；不同意时退出且不收集任何信息
4. [ ] 联系电话入口已改为**选填**（`contact`、发布需求）；发布与保存不再校验手机号必填
5. [ ] `__usePrivacyCheck__: true` 已在 app.json（由 `app.config.ts` 输出）
6. [ ] 真机（或开发者工具切基础库 ≥ 2.32.3）验证弹窗流程

---

## 四、如何检验（验收步骤）

1. 开发者工具 → 右上角“详情 → 本地设置”：勾选“不校验合法域名…”，基础库版本切到 **≥ 2.33.0**；
2. 工具栏“清缓存 → 清除全部缓存”（把本地隐私授权一并清掉）；
3. 重新编译，**首次进入时**应依次出现：

   - 预期 ①：自定义「隐私保护提示」弹窗（含《用户信息采集与隐私保护声明》入口、手机号选填说明）；
   - 预期 ②：点「不同意」→ 弹窗提示后退出小程序；不产生任何信息收集；
   - 预期 ③：点「同意并继续」→ 触发微信官方《用户隐私保护指引》弹窗，同意后进入登录流程；
   - 预期 ④：再次冷启动（不清缓存）不再弹自定义门与官方弹窗；

4. 验证“手机号不强制”：进入“个人信息”页与“发布需求”页，**不填手机号可直接保存/提交**；只在填写了非法号码时才提示格式错误；
5. 如需确认同意态被持久化：清缓存前先同意一次，再 `console` 打印 `wx.getPrivacySetting` 应得 `needAuthorization: false`；
6. 真机预览复测一次（部分机型对弹窗呈现有差异），并确认真机基础库版本 ≥ 2.32.3。

> 常见问题：
> - 若自定义弹窗后未再弹官方弹窗：多为后台指引未“生效”或基础库过低/清缓存没清干净；
> - 若 `requirePrivacyAuthorize` fail 提示需配置指引：按 §2 去后台补配并提交审核生效；
> - H5 浏览器预览无弹窗属正常（仅小程序端接入）。
